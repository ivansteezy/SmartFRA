
export abstract class ServicesEndpoints {
    // to do: once database script is created we can write down all the endpoints that represent all the queries that are neccesary.
    public static GetAllServices: string = "http://localhost:3000/services/AllServices";
    public static InsertService: string = "http://localhost:3000/services/ServiceRegistry";

}