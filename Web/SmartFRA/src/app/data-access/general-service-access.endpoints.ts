
export abstract class GeneralServiceAccessEndpoints {
    // to do: once database script is created we can write down all the endpoints that represent all the queries that are neccesary.
    public static GetAllGeneralServiceAccess: string = "http://localhost:3000/general-service-access/AllGeneralServiceAccess";

}